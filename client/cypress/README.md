# E2E Tests

To run these tests you need to:

1. Build the front-end. `yarn client:build`
2. Have the back-end running `docker compose watch` (or up).
3. Make sure the back-end is serving the front-end. Check that `localhost:3000` serves the front-end.
4.

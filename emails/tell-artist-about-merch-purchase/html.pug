extends ../layout.pug

block content
    div
      div.mw600
        p 
          | Hello #{artist.user.name}!
        p 
          | Someone (#{email}) bought something from you!.
      ul.mw600
        each purchase in purchases 
          li #{purchase.merch.title} amount #{purchase.quantity}:
            | #{purchase.currencyPaid.toUpperCase()} #{(purchase.amountPaid / 100).toFixed(2)}
            | Mirlo's cut: (#{purchase.currencyPaid.toUpperCase()} #{(purchase.platformCut / 100).toFixed(2)})
            strong 
              | Total: #{purchase.currencyPaid.toUpperCase()} #{(purchase.artistCut / 100).toFixed(2)} 

      if message 
        p
          | The buyer left a message for you: "#{message}"
      p
        | To learn more about this order, please see your
        a(href="https://mirlo.space/fulfillment") fulfillment dashboard
        | .
      p The final money will be available via your Stripe dashboard
      p If you have any questions please reach out to hi@mirlo.space


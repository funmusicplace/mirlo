| Hi!
| Someone signed up to Mirlo using this email address
|
| #{user.email}
|
| Click this link to confirm: #{host}/auth/password-reset/confirmation/#{token}?email=#{encodeURIComponent(user.email)}&redirectClient=#{redirectClient}
| 
| If this was not you, you can ignore this email.
= `Mirlo: A ${isNewSubscription ? 'new' : ''} ${interval === 'MONTH' ? 'monthly' : 'yearly'} supporter at ${artist.user.currency.toUpperCase()} ${(artistUserSubscription.amount / 100).toFixed(2)} - ${artistUserSubscription.artistSubscriptionTier.name} tier`

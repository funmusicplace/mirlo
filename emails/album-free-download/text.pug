| Woohoo!
|
| Here's your free download of #{trackGroup.title} by #{trackGroup.artist.name}.
|
| #{trackGroup.title}
| #{host}/#{trackGroup.id}/download?token=#{token}&email=#{email}
| 
| Consider supporting #{trackGroup.artist.name} directly with a financial contribution as well.
| Enjoy!
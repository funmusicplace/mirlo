name: Tests

on:
  push:
    branches:
      - main
      - features/**
      - dependabot/**
  pull_request:
    branches:
      - main

jobs:
  install:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Enable Corepack
        run: corepack enable

      - name: Install node
        uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: "yarn"

      - name: Copy env file
        run: |
          cp .env.example .env

      - name: Install dependencies
        run: yarn install --immutable

      - name: Build client
        run: yarn client:build

      # Build the client and save the build folder as an artifact
      - name: Save build folder
        uses: actions/upload-artifact@v4
        with:
          name: build
          if-no-files-found: error
          path: client/dist

  client-unit:
    needs: install
    timeout-minutes: 10
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Enable Corepack
        run: corepack enable

      - name: Download the build folder
        uses: actions/download-artifact@v4
        with:
          name: build
          path: client/dist

      - name: Run tests
        run: yarn client:test

  api-unit:
    timeout-minutes: 10
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Copy env file
        run: |
          cp .env.example .env

      - name: Build docker image
        run: docker compose build test

      - name: Run tests
        run: docker compose run test

      - name: Print logs
        if: always()
        run: |
          echo "::group::Log output: blackbird-api-test"
          docker compose logs api-test
          echo "::endgroup::"

      - name: Stop containers
        if: always()
        run: docker compose --profile=test down

  cypress-run:
    needs: install
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Enable Corepack
        run: corepack enable

      - name: Download the build folder
        uses: actions/download-artifact@v4
        with:
          name: build
          path: client/dist

      # and run all Cypress tests
      - name: Cypress run
        uses: cypress-io/github-action@v6
        with:
          build: yarn client:build
          start: yarn client:start
          folder: client/cypress
          browser: chrome
          config-file: client/cypress.config.ts
